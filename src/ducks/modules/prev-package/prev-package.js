// Actions

// Action Creators

// Init State

const initState = {
  string: "",
  object: null
};

// Reducer

export default function prevPackageReducer(state = initState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}

// Reducer Functions
